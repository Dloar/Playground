// a small C++ program
#include<iostream>
/* The name iostream suggests support for sequential, or stream, input-output,
 rather than random-access or graphical input-output. */
//using namespace std;
int main()
{
    std::cout << "This (\") is a quote, and this (\\) is a backslash." << std::endl;
    return 0;
}